{
  "name": "chat-app-microservices",
  "version": "1.0.0",
  "description": "Microservices backend for Chat App",
  "private": true,
  "scripts": {
    "start": "docker-compose up --build",
    "stop": "docker-compose down"
  }
}
